<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.onlineshoping">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

  <supports-screens android:smallScreens="true"

        android:normalScreens="true"

        android:largeScreens="true"

        android:xlargeScreens="true"

        android:anyDensity="true"

        android:resizeable="true"

        />

    <application
        android:screenOrientation="portrait"
        android:allowBackup="true"
        android:cantSaveState="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineShoping"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ReviewPage"></activity>
        <activity android:name=".Activities.OrdersPage" />
        <activity android:name=".Activities.BuyPage" />
        <activity android:name=".Activities.AddCartList" />
        <activity android:name=".Activities.GetEmail" />
        <activity android:name=".Activities.ForgetPassword" />
        <activity android:name=".Activities.SettingPage" />
        <activity android:name=".Activities.RegistrationPage" />
        <activity android:name=".Activities.LoginPage" />
        <activity android:name=".Activities.AllComments" />
        <activity android:name=".Activities.ZoomImagePage" />
        <activity android:name=".Activities.MainActivity1" />
        <activity android:name=".Activities.SearchPage" />
        <activity
            android:name=".Activities.MainActivity3"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".Activities.ProductDetailPage" />
        <activity android:name=".Activities.ByCategoryListShow" />
        <activity android:name=".Activities.HomePage" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>